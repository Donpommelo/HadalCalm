package com.mygdx.hadal.constants;

/**
 * Z-Axis Layers that entities can be added to. ATM, there is just 1 for hitboxes beneath everything else
 * and effect for above everything else
 */
public enum ObjectLayer {
    STANDARD,
    HBOX,
    EFFECT
}

package com.mygdx.hadal.event;

/**
 * This enum holds the different types of pickup events that can be synced between server and client
 * @author Zachary Tu
 *
 */
public enum PickupType {
	WEAPON,
	ACTIVE,
}
